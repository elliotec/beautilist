<%= form_for(@list) do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br/>

  <%#= f.fields_for :steps do |builder| %>
    <%#= render 'step_fields', f: builder %>
  <%# end %>
    <% @list.children.each do |sub| %>
      <%= f.fields_for :children, sub do |child| %>
        <%#= child.text_field :name %><br>
        <%#= f.hidden_field :_destroy %>
        <%#= link_to "remove", '#', class: "remove_fields" %><br>
        <%= link_to_add_fields "Add", f, :children %><br>
        <%#= f.select :parent_id, nested_set_options(List, @list) {|i| "#{'-' * i.level} #{i.name}" } %><br>
      <% end %>
    <% end %>


<%= f.submit class: "btn btn-success"%>
<% end %>
